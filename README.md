YOGA POSTURE DETECTION AND CORRECTION

# installation comments
 cd yogapose-main
# Create Env
python -m venv .venv

# Activate Env
.venv\Scripts\activate

#Install Requirements
pip install -r requirements.txt

cd poses
# run
python app.py
